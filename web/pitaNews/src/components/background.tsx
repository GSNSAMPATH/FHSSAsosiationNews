import React from 'react';
import './Flower.css'; // Assuming the CSS is in a separate file

const Flower: React.FC = () => {
  return (
    <div className="flower">

    </div>
  );
};

export default Flower;
